function arrayPlusArray(arr1, arr2) {
  let som = 0;
  for (let i = 0; i < arr1.length; i++) {
    som = som + arr1[i];
  }
  for (let b = 0; b < arr2.length; b++) {
    som = som + arr2[b];
  }
  return som; //something went wrong
}
